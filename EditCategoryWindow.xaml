<Window x:Class="GESTION_DES_STOCKS.EditCategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GESTION_DES_STOCKS"
        mc:Ignorable="d"
        Title="EditCategoryWindow" Height="450" Width="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Champ Nom de la Catégorie -->
        <TextBlock Text="Nom de la Catégorie" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBox x:Name="CategoryNameTextBox" Grid.Row="1" Width="300" Margin="0,0,0,15"/>

        <!-- Champ Description -->
        <TextBlock Text="Description" Grid.Row="2" Margin="0,0,0,5"/>
        <TextBox x:Name="DescriptionTextBox" Grid.Row="3" Width="300" Margin="0,0,0,15"/>

        <!-- Bouton Enregistrer -->
        <Button Content="Enregistrer" Grid.Row="4" Width="100" Height="30" HorizontalAlignment="Right" Click="SaveButton_Click"/>
    </Grid>
</Window>
