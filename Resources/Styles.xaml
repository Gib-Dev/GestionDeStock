<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!-- Styles pour les boutons -->
    <Style TargetType="Button">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Margin" Value="10"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Background" Value="#4CAF50"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#388E3C"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <!-- Couleurs globales -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="#6A00F4" />
    <SolidColorBrush x:Key="SecondaryBrush" Color="#4CAF50" />
    <SolidColorBrush x:Key="AccentBrush" Color="#E53935" />
    <SolidColorBrush x:Key="BackgroundBrush" Color="#EEE2DF" />
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="#EED7C5" />

    <!-- Styles pour les TextBlocks -->
    <Style TargetType="TextBlock" x:Key="TitleText">
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,20,0,10"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="SectionTitle">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,5,0,2"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="SectionSubtitle">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Foreground" Value="Gray"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <!-- Style pour les icônes Material Design -->
    <Style TargetType="{x:Type materialDesign:PackIcon}" x:Key="IconStyle">
        <!-- Ajustez les dimensions de l'icône si nécessaire ici -->
    </Style>

    <!-- Style pour les cartes dans le tableau de bord -->
    <Style TargetType="Border" x:Key="CardStyle">
        <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Gray" BlurRadius="8" ShadowDepth="2"/>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
